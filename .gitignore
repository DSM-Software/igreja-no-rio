# Dependências
node_modules/

# Build
.next/
out/

# Env — NUNCA commitar arquivos de ambiente com secrets
.env
.env.local
.env.*.local
.env.production
# O .env.example e .env.production.example SÃO versionados (sem secrets reais)

# SSL / certificados — nunca versionados
nginx/certs/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
tsconfig.tsbuildinfo
next-env.d.ts

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker volumes (gerados localmente)
docker-volumes/

# Payload generated (regenerado com `npm run generate:types`)
# src/payload-types.ts  ← comentado: queremos versionar o arquivo gerado

# Playwright
playwright-report/
test-results/

# Playwright MCP — logs/snapshots temporários, nunca versionados
.playwright-mcp/

# Screenshots ad-hoc de inspeção visual na raiz — nunca versionados
# (imagens de conteúdo ficam em public/ e SÃO versionadas)
/*.png
